import '../../styles/components/buttons.css';

export default function LoginButton() {
    return (
        <div className='container-btn-login'>
            <button type='submit' id='login-btn' className="buttons">Login</button>
        </div>
    )
}